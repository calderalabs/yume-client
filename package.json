{
  "name": "yume-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bs-director": "^0.0.6",
    "bs-fetch": "0.2.0",
    "bs-json": "0.2.4",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "reason-scripts": "0.8.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prepare": "npm link bs-platform",
    "deploy": "npm run build && aws s3 sync build/ s3://yume-client-production"
  },
  "proxy": "http://localhost:3001",
  "devDependencies": {
    "bs-jest": "^0.2.0",
    "reason-react": "^0.3.0"
  }
}
